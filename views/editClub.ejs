<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Club Details</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            flex-direction: column;
        }

        h2 {
            font-size: 2.5rem;
            color: #fff;
            margin-bottom: 30px;
        }

        .form-container {
            background-color: #fff;
            color: #333;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
            text-align: center;
        }

        label {
            font-size: 1.1rem;
            margin-bottom: 8px;
            display: block;
            text-align: left;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1rem;
            transition: 0.3s;
        }

        input[type="text"]:focus {
            border-color: #2575fc;
            outline: none;
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #2575fc;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 1.2rem;
            cursor: pointer;
            transition: 0.3s;
        }

        button:hover {
            background-color: #6a11cb;
        }

        .back-home {
            margin-top: 20px;
            font-size: 1.1rem;
        }

        .back-home a {
            color: #2575fc;
            text-decoration: none;
        }

        .back-home a:hover {
            text-decoration: underline;
        }

    </style>
</head>
<body>
    <h2>Edit Details for <%= club.name %></h2>
    <div class="form-container">
        <form action="/edit-club/<%= club._id %>" method="POST">
            <label for="name">Club Name:</label>
            <input type="text" id="name" name="name" value="<%= club.name %>" required><br>

            <label for="motto">Motto:</label>
            <input type="text" id="motto" name="motto" value="<%= club.motto %>" required><br>

            <label for="chairman">Chairman:</label>
            <input type="text" id="chairman" name="chairman" value="<%= club.roles.chairman %>" required><br>

            <label for="secretary">Secretary:</label>
            <input type="text" id="secretary" name="secretary" value="<%= club.roles.secretary %>" required><br>

            <label for="president">President:</label>
            <input type="text" id="president" name="president" value="<%= club.roles.president %>" required><br>

            <button type="submit">Update Club</button>
        </form>
    </div>

    <div class="back-home">
        <a href="/home">Back to Home</a>
    </div>
</body>
</html>
