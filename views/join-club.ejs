<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clubs of GECI</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
    
        body {
            background-image: url('/images/view-clubs-background.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            font-family: 'Roboto', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .content {
            width: 100%;
            max-width: 1200px;
            text-align: center;
        }

        h1 {
            text-align: center;
            margin: 20px 0;
            color: #333;
        }

        .search-bar {
            margin-bottom: 20px;
        }

        .club-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            padding: 20px;
        }

        .club-card {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            width: 250px;
            padding: 15px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            text-align: center;
            transition: transform 0.3s ease;
        }

        .club-card:hover {
            transform: translateY(-5px);
        }

        .club-card h3 {
            font-size: 1.5rem;
            color: #333;
            margin-bottom: 10px;
        }

        .club-card p {
            font-size: 1rem;
            color: #777;
            margin-bottom: 15px;
        }

        .club-card a {
            display: block;
            background-color: #007bff;
            color: #fff;
            padding: 8px;
            margin: 5px 0;
            text-decoration: none;
            border-radius: 4px;
        }

        .club-card a:hover {
            background-color: #0056b3;
        }

        .back-home {
            text-align: center;
            margin-top: 20px;
        }

        .back-home a {
            background-color: #28a745;
            color: #fff;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 4px;
        }

        .back-home a:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>
    <div class="content">
        <h1>Clubs of GECI</h1>

        <div class="search-bar">
            <form action="/search-clubs" method="GET">
                <input type="text" name="query" placeholder="Search clubs..." required>
                <button type="submit">Search</button>
            </form>
        </div>

        <div class="club-container">
            <% clubs.forEach(club => { %>
                <div class="club-card">
                    <h3><%= club.name %></h3>
                    <p><%= club.motto %></p>
                    <a href="/club-details/<%= club._id %>">See Details</a>
                    <a href="/add-event/<%= club._id %>">Schedule an Event</a>
                </div>
            <% }) %>
        </div>

        <div class="back-home">
            <a href="/home">Back to Home</a>
        </div>
    </div>
</body>
</html>