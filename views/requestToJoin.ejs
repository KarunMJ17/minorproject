<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Request to Join <%= club.name %></title>
    <style>
        /* Global Styling */
          * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-image: url('/images/view-clubs-background.jpg');
            background-size: cover; /* Change this to contain */
            background-repeat: no-repeat;
            background-position: center;
            font-family: 'Roboto', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        /* Main Container Styling */
        .request-container {
            background: #fff;
            padding: 40px 30px;
            border-radius: 12px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
            text-align: center;
            width: 100%;
            max-width: 400px;
            animation: fadeIn 0.8s ease-in-out;
        }

        h1 {
            color: #333;
            margin-bottom: 20px;
            font-size: 1.8em;
        }

        label {
            display: block;
            margin-bottom: 10px;
            font-weight: 600;
            color: #555;
            text-align: left;
        }

        /* Input Styling */
        input[type="text"] {
            width: 100%;
            padding: 12px 15px;
            margin: 10px 0 20px 0;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 1em;
            transition: border-color 0.3s;
        }

        input[type="text"]:focus {
            outline: none;
            border-color: #2575fc;
            box-shadow: 0 0 5px rgba(37, 117, 252, 0.3);
        }

        /* Button Styling */
        button {
            background-color: #2575fc;
            color: #fff;
            border: none;
            padding: 12px;
            width: 100%;
            font-size: 1em;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.2s;
        }

        button:hover {
            background-color: #6a11cb;
            transform: translateY(-2px);
        }

        /* Responsive Design */
        @media (max-width: 400px) {
            .request-container {
                padding: 20px;
            }
        }

        /* Fade-in animation */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
    <div class="request-container">
        <h1>Request to Join <%= club.name %></h1>
        <form action="/submit-request/<%= club._id %>" method="POST">
            <label for="name">Your Name:</label>
            <input type="text" id="name" name="name" required>
            <button type="submit">Submit Request</button>
        </form>
    </div>
</body>
</html>
