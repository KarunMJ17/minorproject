<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Club Details</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            background-image: url('/images/view-clubs-background.jpg');
            background-size: cover; /* Change this to contain */
            background-repeat: no-repeat;
            background-position: center;
            font-family: 'Roboto', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            background-color: #e5e9e6;
            color: #333;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 600px;
            overflow-y: auto;
            max-height: 80vh;
        }
        h1 {
            font-size: 2rem;
            color: #2575fc;
            text-align: center;
            margin-bottom: 20px;
        }
        .club-card {
            background-color: #f3f4f6;
            border: 1px solid #ddd;
            padding: 20px;
            margin-bottom: 15px;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            text-align: left;
        }
        .club-card h2 {
            font-size: 1.5rem;
            color: #333;
            margin-bottom: 10px;
        }
        .club-card p {
            font-size: 1rem;
            margin: 5px 0;
        }
        .club-card strong {
            color: #2575fc;
        }
        .schedule-btn, .request-btn, .back-btn {
            display: inline-block;
            margin-top: 10px;
            padding: 10px 20px;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            text-align: center;
            transition: background-color 0.3s ease;
        }
        .schedule-btn { background-color: #2575fc; }
        .schedule-btn:hover { background-color: #6a11cb; }
        .request-btn { background-color: #28a745; }
        .request-btn:hover { background-color: #218838; }
        .back-btn { background-color: #333; margin-top: 20px; }
        .back-btn:hover { background-color: #555; }
        .requests-list {
            margin-top: 15px;
            padding: 10px;
            background-color: #e9ecef;
            border-radius: 5px;
            color: #333;
        }
        .requests-list p {
            margin: 5px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Club Details</h1>
        <% if (club) { %>
            <div class="club-card">
                <h2><%= club.name %></h2>
                <p><strong>Motto:</strong> <%= club.motto %></p>
                <p><strong>Chairman:</strong> <%= club.roles.chairman %></p>
                <p><strong>Secretary:</strong> <%= club.roles.secretary %></p>
                <p><strong>President:</strong> <%= club.roles.president %></p>
                
                <!-- Request to Join Button -->
                <a href="/request-to-join/<%= club._id %>">
                    <button class="request-btn">Request to Join</button>
                </a>

                <!-- Display list of requests -->
                <div class="requests-list">
                    <h3>Join Requests</h3>
                    <% if (requestedUsers && requestedUsers.length > 0) { %>
                        <% requestedUsers.forEach(function(request) { %>
                            <p><%= request.name %></p>
                        <% }); %>
                    <% } else { %>
                        <p>No requests yet.</p>
                    <% } %>
                </div>
                
            </div>
        <% } else { %>
            <p>No club found.</p>
        <% } %>

        <a href="/home" class="back-btn">Back to Home</a>
    </div>
</body>
</html>
