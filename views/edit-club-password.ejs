<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Club Password</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
            color: #fff;
            overflow: hidden;
        }

        .container {
            width: 90%;
            max-width: 600px;
            background: rgba(25, 25, 50, 0.9);
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 8px 30px rgba(255, 0, 102, 0.3);
            text-align: center;
            overflow: hidden;
        }

        h1 {
            margin-bottom: 20px;
            font-size: 2rem;
            color: #ff0066;
            text-shadow: 0 0 10px #ff0066, 0 0 20px #ff3399;
        }

        .search-bar {
            margin-bottom: 20px;
        }

        .search-bar form {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .search-bar input {
            width: 70%;
            padding: 10px;
            border: 1px solid #444;
            border-radius: 8px 0 0 8px;
            font-size: 1rem;
            outline: none;
            background: #1c1c3c;
            color: #fff;
        }

        .search-bar input:focus {
            border-color: #ff0066;
        }

        .search-bar button {
            padding: 10px 15px;
            background: linear-gradient(90deg, #ff0066, #ff3399);
            color: white;
            border: none;
            border-radius: 0 8px 8px 0;
            font-size: 1rem;
            cursor: pointer;
            text-shadow: 0 0 5px #ff0066, 0 0 10px #ff3399;
        }

        .search-bar button:hover {
            background: linear-gradient(90deg, #ff3399, #ff0066);
            box-shadow: 0 0 15px #ff3399, 0 0 25px #ff0066;
        }

        .club-list {
            max-height: 300px;
            overflow-y: auto;
            border-top: 1px solid #444;
            padding-top: 10px;
            background: rgba(0, 0, 0, 0.5);
            border-radius: 8px;
        }

        .club-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px;
            border-bottom: 1px solid #333;
        }

        .club-item:last-child {
            border-bottom: none;
        }

        .club-item span {
            flex: 1;
            text-align: left;
            font-size: 1rem;
            color: #ccc;
        }

        .club-item input[type="password"] {
            width: 150px;
            padding: 8px;
            border: 1px solid #444;
            border-radius: 8px;
            font-size: 0.9rem;
            background: #1c1c3c;
            color: #fff;
        }

        .club-item input:focus {
            border-color: #ff0066;
        }

        .club-item button {
            padding: 8px 12px;
            background: linear-gradient(90deg, #28a745, #34d058);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 0.9rem;
            cursor: pointer;
            text-shadow: 0 0 5px #28a745, 0 0 10px #34d058;
        }

        .club-item button:hover {
            background: linear-gradient(90deg, #34d058, #28a745);
            box-shadow: 0 0 15px #34d058, 0 0 25px #28a745;
        }

        /* Scrollbar customization */
        .club-list::-webkit-scrollbar {
            width: 8px;
        }

        .club-list::-webkit-scrollbar-thumb {
            background-color: #666;
            border-radius: 4px;
        }

        .club-list::-webkit-scrollbar-thumb:hover {
            background-color: #888;
        }

        .back-home {
            margin-top: 20px;
            font-size: 1rem;
            position: absolute;
            bottom: 20px;
        }

        .back-home a {
            color: #ff0066;
            text-decoration: none;
            font-size: 1.1rem;
            text-shadow: 0 0 5px #ff0066, 0 0 10px #ff3399;
        }

        .back-home a:hover {
            text-shadow: 0 0 10px #ff0066, 0 0 20px #ff3399;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Select Club to Edit</h1>

        <!-- Search Bar -->
        <div class="search-bar">
            <form action="/search-edit-clubs" method="GET">
                <input type="text" name="query" placeholder="Search clubs..." required>
                <button type="submit">Search</button>
            </form>
        </div>

        <!-- Club List -->
        <div class="club-list">
            <% clubs.forEach(club => { %>
                <div class="club-item">
                    <span><%= club.name %></span>
                    <form action="/edit-club-password" method="POST" style="display: flex; align-items: center;">
                        <input type="hidden" name="clubId" value="<%= club._id %>">
                        <input type="password" name="password" placeholder="Enter Password" required>
                        <button type="submit">Confirm</button>
                    </form>
                </div>
            <% }) %>
        </div>
    </div>

    <div class="back-home">
        <a href="/home">Back to Home</a>
    </div>    
</body>
</html>
