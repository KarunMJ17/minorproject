<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Schedule an Event</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            flex-direction: column;
            overflow: hidden;
        }

        h1 {
            font-size: 2.8rem;
            color: #ff0066;
            margin-bottom: 20px;
            text-shadow: 0 0 10px #ff0066, 0 0 20px #ff3399;
            animation: fadeIn 1s ease-in-out;
        }

        .form-container {
            background: rgba(25, 25, 50, 0.9);
            color: #fff;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 30px rgba(255, 0, 102, 0.3);
            width: 90%;
            max-width: 500px;
            text-align: center;
            animation: slideIn 1s ease-in-out;
        }

        label {
            font-size: 1.1rem;
            margin-bottom: 8px;
            display: block;
            text-align: left;
            color: #ccc;
        }

        input[type="password"], input[type="text"], input[type="date"], input[type="number"] {
            width: 100%;
            padding: 12px;
            margin-bottom: 20px;
            border: 1px solid #444;
            border-radius: 8px;
            font-size: 1rem;
            transition: 0.3s;
            background: #1c1c3c;
            color: #fff;
        }

        input:focus {
            border-color: #ff0066;
            outline: none;
            background: #24244d;
        }

        button {
            width: 100%;
            padding: 12px;
            background: linear-gradient(90deg, #ff0066, #ff3399);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1.2rem;
            cursor: pointer;
            transition: all 0.3s ease;
            font-weight: bold;
            text-shadow: 0 0 5px #ff0066, 0 0 10px #ff3399;
        }

        button:hover {
            background: linear-gradient(90deg, #ff3399, #ff0066);
            box-shadow: 0 0 15px #ff3399, 0 0 25px #ff0066;
        }

        #addEventForm {
            display: none;
        }

        .back-home {
            margin-top: 20px;
            font-size: 1rem;
        }

        .back-home a {
            color: #ff0066;
            text-decoration: none;
        }

        .back-home a:hover {
            text-shadow: 0 0 10px #ff0066, 0 0 20px #ff3399;
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <h1>Protected Add Event Page</h1>
    <div class="form-container">
        <div id="passwordForm">
            <form>
                <label for="password">Enter Password of the club:</label>
                <input type="password" id="password" placeholder="Enter password" required>
                <button type="button" onclick="checkPassword()">Submit</button>
            </form>
        </div>

        <div id="addEventForm">
            <form action="/add-event/<%= clubId %>" method="POST">
                <label for="eventName">Event Name:</label>
                <input type="text" id="eventName" name="eventName" required><br>

                <label for="eventDate">Event Date:</label>
                <input type="date" id="eventDate" name="eventDate" required><br>

                <label for="eventDays">Number of Days:</label>
                <input type="number" id="eventDays" name="eventDays" min="1" required><br>
                
                <button type="submit">Add Event</button>
            </form>
        </div>
    </div>
    <div class="back-home">
        <a href="/home">Back to Home</a>
    </div>

    <script>
        function checkPassword() {
            const password = document.getElementById('password').value;
            if (password === 'science') {
                document.getElementById('passwordForm').style.display = 'none';
                document.getElementById('addEventForm').style.display = 'block';
            } else {
                alert('Incorrect password! Please try again.');
            }
        }
    </script>
</body>
</html>
